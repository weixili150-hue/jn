services:
  - type: web
    name: entropy-observer
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
      - key: PORT
        value: "10000"
